---
page_title: Stefan's Blog
pageable: false
per_page: 10
---
<h1>Render this page!</h1>


<% page_articles.each_with_index do |article, i| %>
  <h2><%= link_to article.title, article %> <span><%= article.date.strftime('%b %e') %></span></h2>
  <%= article.body %>
<% end %>
  
  <p>
    ```
    puts "i"
    ```
  </p>
  ```
  puts "i"
  ```
  
  <p>puts "i":</p>
  <%= puts "i" %>
  <p><%= puts "i in p tags" %></p>
  <p>puts ["hi", "there"]:</p>
  <%= puts ["hi", "there"] %>
  <p><%= puts ["in", "p tags"] %></p>
  
  <p> p :</p>
  <%= p "i" %>
  <p><%= p "i in p tags" %></p>
  
  <p>"i":</p>
  <%= "i" %>
  
  <p>ap "i"</p>
  <%= ap "i" %>
<h3>after all? Everything above is yielded.</h3>