---
page_title: Stefan's Notes
pageable: false
per_page: 10
---
<div id="article-title">
    <h2>On Notes and The Jargon File</h2>
</div>

    <% blog('scribbles').articles.each do |article| %>
        <% if article.title == "The Jargon File" %>
            <%= article.body %>
        <% end %>
    <% end %>

<div id="notes-listing">
    <ul class="compact">
    <% blog('notes').articles.each do |article| %>
        <li> <%= link_to article.title, article %> </li>
    <% end %>
    </ul>
</div>
